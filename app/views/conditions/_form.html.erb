<%= form_for(@condition) do |f| %>
  <% if @condition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@condition.errors.count, "error") %> prohibited this condition from being saved:</h2>

      <ul>
      <% @condition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-horizontal">
    <div class="form-group">
      <%= f.label :name, :class => "col-md-2 control-label" %>
      <%= f.text_field :name, :class => "co-md-10 form-control-static"%>
    </div>
    <div class="form-group">
      <%= f.label :gold, :class => "col-md-2 control-label" %>
      <%= f.text_field :gold, :class => "co-md-10 form-control-static"%>
    </div>
    <div class="form-group">
      <%= f.label :silver, :class => "col-md-2 control-label" %>
      <%= f.text_field :silver, :class => "co-md-10 form-control-static"%>
    </div>
    <div class="form-group">
      <%= f.label :bronze, :class => "col-md-2 control-label" %>
      <%= f.text_field :bronze, :class => "co-md-10 form-control-static"%>
    </div>
    <div class="form-group">
      <%= f.label :description, :class => "col-md-2 control-label" %>
      <%= f.text_field :description, :class => "co-md-10 form-control-static"%>
    </div>
    <div class="form-group">
      <%= f.label :image_name, :class => "col-md-2 control-label" %>
      <%= f.text_field :image_name, :class => "co-md-10 form-control-static"%>
    </div>
    <div class="form-group">
      <div class="col-md-offset-2">
        <%= f.submit %>
      </div>
    </div>
  </div>
<% end %>
